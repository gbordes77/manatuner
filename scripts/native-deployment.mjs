// Deliberately does not deploy: the GitHub integration owns production builds.
console.log(`ManaTuner publishes through Vercel's native GitHub integration.
After validating a commit, push it to main and inspect that commit's Vercel check.
Do not run a second CLI deployment.
Deployment checks: https://github.com/gbordes77/manatuner/commits/main/
Rollback: select a previous production deployment in the Vercel dashboard.
See docs/deployment/NATIVE-DEPLOYMENT.md.`)
