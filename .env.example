# ManaTuner Pro - Environment Variables Example
# Copy this file to .env.local and fill in your values

# ===========================================
# REQUIRED VARIABLES
# ===========================================

# None - ManaTuner Pro works in Privacy-First mode by default

# ===========================================
# OPTIONAL VARIABLES
# ===========================================

# Error Monitoring (optional - disabled when DSN is unset; actual deployment must be checked)
# Before enabling, follow docs/privacy/DATA-FLOWS.md (payload review, retention, legal review and disclosures).
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ===========================================
# NOTES
# ===========================================
# - All VITE_ prefixed variables are exposed to the client
# - Never commit actual values to git
# - Test the app without optional variables to ensure graceful degradation
# - For Vercel deployment, add these in Project Settings > Environment Variables
# - Calculations run locally; uncached card lookups, images and fonts require external services.
