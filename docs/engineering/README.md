# Engineering docs

Architecture, castabilité, mulligan, land system, guides de dev.

**Runtime 2026-08-01 (v2.7.7) :** `DeckCard.etbTapped` is a **boolean** (not a function). See `../product/STATUS.md`. Some older design notes in this folder may still describe the legacy function shape — prefer code + STATUS.
