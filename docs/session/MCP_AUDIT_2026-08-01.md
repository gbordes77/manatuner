# Audit MCP expert — ManaTuner

> **Date :** 2026-08-01  
> **Phase :** 1 — AUDIT + PROPOSITION uniquement (aucune installation effectuée)  
> **Agents couverts :** Grok Build (session courante) + Claude Code (configs user/projet)  
> **Prompt source :** `docs/session/PROMPT_MCP_EXPERT_AUDIT.md`  
> **Prod :** v2.7.8 · https://www.manatuner.app · repo `gbordes77/manatuner`

---

## 0. Synthèse exécutive (30 secondes)

| Question              | Réponse                                                                                                  |
| --------------------- | -------------------------------------------------------------------------------------------------------- |
| MCP sains aujourd’hui | **context7** (docs libs) · **apify** (scraping Actors)                                                   |
| MCP en panne          | **agentvibes** (handshake fail — Python `mcp` manquant)                                                  |
| Config Grok user      | **0** serveur dans `~/.grok/config.toml` (découverte via compat Claude + `.mcp.json`)                    |
| Config Claude user    | **2** HTTP dans `~/.claude.json` (context7, apify)                                                       |
| Config projet         | `.mcp.json` → agentvibes **uniquement**                                                                  |
| Plus gros gain P0     | **Playwright MCP** (audits personas / smoke UI) + **Vercel MCP** (logs/deploy) + **GitHub MCP** (PRs/CI) |
| À ne pas toucher      | Sentry MCP tant que DSN off · SEO MCP (skills déjà massives) · analytics decklist                        |
| **Alerte sécu**       | Clés API Context7 + Apify en **clair** dans `~/.claude.json` → **rotation recommandée**                  |

**Décision attendue de l’humain :**  
`go install` · `go install: P0` · `go install: nom1,nom2` · `stop`

---

## 1. Contexte projet (jobs réels)

| Job                                        | Outil / stack actuel                                              | Besoin MCP ?                          |
| ------------------------------------------ | ----------------------------------------------------------------- | ------------------------------------- |
| Dev frontend React 18 + MUI + Vite + Redux | npm local + skills docs                                           | Context7 **déjà OK**                  |
| Unit tests                                 | Vitest (`npm run test:unit` — 381 pass)                           | Non (CLI)                             |
| E2E / personas / a11y                      | Playwright local + skills a11y                                    | **Oui** — MCP browser interactif      |
| Deploy prod                                | Vercel (`npm run build:vercel`, CLI via npx)                      | **Oui** — logs / status OAuth         |
| Git / PRs                                  | `gh` authentifié (`gbordes77`, scopes repo+workflow)              | Utile (MCP > CLI pour multi-step)     |
| Docs libs à jour                           | Context7 HTTP + skill `context7-mcp` / `find-docs` / règle `ctx7` | Déjà couvert                          |
| Distribution X                             | **Grok native** `x_*` tools + `LAUNCH.md`                         | Pas de MCP X sur Grok                 |
| Distribution Discord                       | Manuel (LAUNCH.md)                                                | Optionnel bot (setup lourd)           |
| SEO / GEO Library                          | **50+ skills** SEO/GEO locales                                    | **Skip MCP** (doublon)                |
| Privacy / Sentry                           | SDK installé, DSN **off** (`SECURITY.md`)                         | **Interdit** sans owner               |
| Scryfall / deck data                       | Client-side only                                                  | **Pas de MCP** qui exfiltre decklists |
| Scraping metagame / creators               | Apify **déjà**                                                    | OK si pas de PII decklist             |
| TTS agent                                  | AgentVibes dans `.mcp.json`                                       | Cassé → fix ou retirer                |

**Priorité business (LAUNCH.md) :** distribution, pas feature gratuite.

---

## 2. État actuel des MCP

### 2.1 Tableau inventaire

| Source                                | Serveur                                        | Statut                  | Outils (aperçu)                     | Notes                                                                |
| ------------------------------------- | ---------------------------------------------- | ----------------------- | ----------------------------------- | -------------------------------------------------------------------- |
| Session Grok (runtime)                | **context7**                                   | **ok**                  | resolve-library-id, query-docs (≈2) | Via compat `~/.claude.json` HTTP                                     |
| Session Grok (runtime)                | **apify**                                      | **ok**                  | 15 tools (Actors, store, datasets…) | Via compat `~/.claude.json` HTTP                                     |
| Session Grok (runtime)                | **agentvibes**                                 | **fail**                | —                                   | `.mcp.json` projet ; stderr : `Failed to install Python mcp package` |
| Session Grok (runtime)                | **tasks**                                      | **ok**                  | 6 tools                             | Intégration native / session Grok (pas dans configs MCP user)        |
| `~/.grok/config.toml`                 | —                                              | empty                   | 0 servers                           | Grok s’appuie sur compat Claude + projet                             |
| `~/.claude.json` → `mcpServers`       | context7                                       | **ok**                  | 2                                   | HTTP `https://mcp.context7.com/mcp` + header API key **en clair**    |
| `~/.claude.json` → `mcpServers`       | apify                                          | **ok**                  | 15                                  | HTTP `https://mcp.apify.com` + Bearer **en clair**                   |
| Projet `.mcp.json`                    | agentvibes                                     | **fail**                | —                                   | `npx -y --package=agentvibes agentvibes-mcp-server`                  |
| Projet Claude `.claude/settings.json` | —                                              | n/a                     | hooks TTS only                      | Pas de `mcpServers` projet Claude                                    |
| Claude `mcp-needs-auth-cache`         | claude.ai Google Drive                         | auth cache              | —                                   | Connecteur cloud Claude.ai, hors Grok                                |
| Claude.ai (historique)                | Gmail, Cloudflare, Notion, Calendar, Canva     | unknown ici             | —                                   | `claudeAiMcpEverConnected` — pas actifs dans cette session Grok      |
| Cursor projet ManaTuner               | —                                              | empty                   | —                                   | `.cursor/rules` only, pas de `mcp.json`                              |
| Skills Claude (remplacent MCP)        | SEO×50+, a11y, context7-mcp, marketing         | **actifs**              | n/a                                 | **Ne pas dupliquer en MCP**                                          |
| Agents locaux `~/.claude/agents`      | 24 (qa-expert, deploy, etc.)                   | **actifs**              | n/a                                 | Orchestration SuperClaude                                            |
| Plugins marketplace Claude            | github, playwright, discord, linear, context7… | **dispo non installés** | n/a                                 | Install via `/plugin` Claude Code                                    |

### 2.2 Diagnostics exécutés

```text
grok mcp list     → "No MCP servers configured"  (user TOML vide)
grok mcp doctor   → 2 healthy (apify, context7) · 1 failing (agentvibes)
claude mcp list   → context7 ✓ · apify ✓ · agentvibes ✗
```

**agentvibes stderr** (`~/.grok/logs/mcp/agentvibes.stderr.log`) :

```text
ERROR: Failed to install Python mcp package. Run: pip install mcp
```

### 2.3 Secrets — action immédiate (hors install MCP)

Les clés **Context7** et **Apify** sont stockées en clair dans `~/.claude.json`.  
Cette session d’audit a lu ce fichier via tooling : **considérer les clés comme exposées** dans l’historique agent.

**Recommandation (à faire manuellement, pas en Phase 1 install) :**

1. **Révoquer / régénérer** la clé Context7 et le token Apify
2. Les stocker en variables d’environnement shell (`CONTEXT7_API_KEY`, `APIFY_TOKEN`)
3. Réécrire les headers MCP pour référencer `${CONTEXT7_API_KEY}` / `${APIFY_TOKEN}` si le client le supporte, sinon laisser hors git dans un fichier non versionné
4. Ne **jamais** committer `~/.claude.json` ni coller ces valeurs dans le repo ManaTuner

---

## 3. Alternatives natives déjà disponibles (anti-doublon)

| Capacité            | Alternative native / skill                                    | Verdict MCP                       |
| ------------------- | ------------------------------------------------------------- | --------------------------------- |
| Docs React/MUI/Vite | Context7 MCP + skill `find-docs` + `npx ctx7`                 | **Garder** context7               |
| Browser E2E scripté | `npm run test:e2e` Playwright local                           | MCP = **interactif** (complément) |
| GitHub              | `gh` CLI auth OK                                              | MCP = multi-step agent UX         |
| Vercel              | `npx vercel` (v32.7.2 dans node_modules)                      | MCP = logs/deploy conversationnel |
| X / Twitter         | Outils Grok `x_keyword_search`, `x_semantic_search`, etc.     | **Pas de MCP X** sur Grok         |
| Web browse          | Grok `web_search` / `open_page`                               | OK                                |
| SEO audit           | skills `cseo-*`, `geo-*`, `seo-*`                             | **Skip** MCP SEO                  |
| a11y                | skills + `@axe-core/playwright`                               | Skip MCP dédié                    |
| Scryfall            | App client + fetch code                                       | Skip MCP                          |
| Memory projet       | `docs/session/HANDOFF_*.md`, CLAUDE.md                        | Memory MCP = nice-to-have bas     |
| TTS                 | hooks `.claude/hooks/session-start-tts.sh` + AgentVibes cassé | Fix ou drop                       |

---

## 4. Propositions MCP (format obligatoire)

### [Playwright MCP — `@playwright/mcp`]

- **Priorité :** **P0**
- **Job couvert :** navigateur interactif pour smoke prod, audits personas, vérif UI post-modif
- **Pourquoi pour CE projet :** Playwright déjà en stack (`@playwright/test` 1.57) ; personas + Joyride + EDH samples demandent des parcours navigateur guidés par l’agent
- **Pourquoi PAS un skill/CLI déjà là :** les tests E2E sont batch ; le MCP permet exploration live (localhost:3000 / manatuner.app) sans écrire un spec
- **Install cible :** projet (`.mcp.json`) **et** user-global (Grok + Claude)
- **Méthode d’install :**

  ```bash
  # Grok (projet)
  cd "/Volumes/DataDisk/_Projects/Project Mana base V2"
  grok mcp add --scope project playwright -- npx -y @playwright/mcp@latest

  # Grok (user)
  grok mcp add playwright -- npx -y @playwright/mcp@latest

  # Claude Code
  claude mcp add playwright -- npx -y @playwright/mcp@latest
  ```

  Snippet `.mcp.json` :

  ```json
  "playwright": {
    "command": "npx",
    "args": ["-y", "@playwright/mcp@latest"]
  }
  ```

- **Auth / secrets :** aucune
- **Privacy / risque :** **OK ManaTuner** si l’agent ne paste pas de decklist utilisateur vers un service tiers ; navigateur local
- **Coût / quota :** free (local)
- **Smoke test post-install :** ouvrir `http://localhost:3000/analyzer?sample=edh` et lire le titre / stamp Engine
- **Décision recommandée :** **Installer**

---

### [Vercel MCP — officiel `https://mcp.vercel.com`]

- **Priorité :** **P0**
- **Job couvert :** status deploy, logs runtime, métadonnées projet Vercel
- **Pourquoi pour CE projet :** prod 100 % Vercel ; prerender soft-fail Chromium ; debug deploy sans quitter l’agent
- **Pourquoi PAS un skill/CLI déjà là :** `npx vercel` existe mais pas d’API tools structurés pour l’agent ; pas de CLI global installé (`vercel not found` hors npx)
- **Install cible :** user-global (multi-projets) ; pas besoin secrets dans le repo
- **Méthode d’install :**

  ```bash
  # Grok
  grok mcp add --transport http vercel https://mcp.vercel.com

  # Claude Code
  claude mcp add --transport http vercel https://mcp.vercel.com
  # puis authentifier (OAuth) via /mcp dans la session Claude
  ```

- **Auth / secrets :** OAuth Vercel (pas de token à committer)
- **Privacy / risque :** **attention** — logs peuvent contenir URLs ; ne pas y coller de decklists ; scope = deploy metadata
- **Coût / quota :** free (compte Vercel existant)
- **Smoke test post-install :** lister le projet manatuner / dernier deployment
- **Décision recommandée :** **Installer**

---

### [GitHub MCP — remote officiel `https://api.githubcopilot.com/mcp/`]

- **Priorité :** **P0** (ou P1 si tu préfères rester 100 % `gh`)
- **Job couvert :** PRs, issues, Actions CI, Dependabot, code search API
- **Pourquoi pour CE projet :** repo `gbordes77/manatuner`, workflow ship fréquent, CI utile post-push
- **Pourquoi PAS un skill/CLI déjà là :** `gh` est excellent et **déjà auth** ; le MCP apporte des tools multi-étapes (triage PR + CI + review) plus fluides pour l’agent. Alternative acceptable : **ne pas installer** et garder `gh`
- **Install cible :** user-global
- **Méthode d’install :**

  ```bash
  # Remote OAuth (préféré si host supporte)
  grok mcp add --transport http github https://api.githubcopilot.com/mcp/
  claude mcp add --transport http github https://api.githubcopilot.com/mcp/

  # Fallback local (PAT) — NE PAS committer le token
  # export GITHUB_PERSONAL_ACCESS_TOKEN=ghp_...
  grok mcp add github -e GITHUB_PERSONAL_ACCESS_TOKEN=${GITHUB_PERSONAL_ACCESS_TOKEN} -- \
    npx -y @modelcontextprotocol/server-github
  ```

- **Auth / secrets :** OAuth GitHub ou PAT (`repo`, `workflow`) ; **jamais** dans `.mcp.json` commité
- **Privacy / risque :** **OK** si pas de decklist dans issues/PR bodies
- **Coût / quota :** free (GitHub account) ; remote peut dépendre policies Copilot/GitHub App host
- **Smoke test post-install :** `list issues` ou `get repo gbordes77/manatuner`
- **Décision recommandée :** **Installer** (remote) ; sinon garder `gh`

---

### [Context7 — déjà installé]

- **Priorité :** P0 (maintenance)
- **Job couvert :** docs à jour React, MUI, Vite, Playwright, Redux…
- **Pourquoi pour CE projet :** stack frontend en mouvement (Vite 7, Vitest 4)
- **Pourquoi PAS un skill déjà là :** skill + MCP = couple utile ; le MCP est la source live
- **Install cible :** déjà user (`~/.claude.json`) ; Grok le charge via compat
- **Méthode d’install :** déjà fait — **action = rotation clé + headers env**
- **Auth / secrets :** API key Context7
- **Privacy / risque :** OK (docs publiques, pas de decklist)
- **Coût / quota :** freemium (quota API)
- **Smoke test :** resolve `react` / query hooks
- **Décision recommandée :** **Garder** + sécuriser la clé

---

### [Apify — déjà installé]

- **Priorité :** P1 (garder, pas étendre)
- **Job couvert :** scraping légitime (metagame, pages créateurs, SERP tiers…) pour **distribution / research**
- **Pourquoi pour CE projet :** LAUNCH.md (fireshoes, créateurs) ; pas pour parser des decklists utilisateurs
- **Pourquoi PAS un skill déjà là :** Actors cloud uniques
- **Install cible :** user-global (déjà)
- **Auth / secrets :** Bearer Apify — **à rotator**
- **Privacy / risque :** **attention** — **interdit** d’y envoyer decklists utilisateurs ManaTuner ; usage = research publique
- **Coût / quota :** freemium / paid selon Actors
- **Smoke test :** list actors / store search
- **Décision recommandée :** **Garder** + policy d’usage + rotation token

---

### [AgentVibes — déjà déclaré, cassé]

- **Priorité :** P1 (fix **ou** retirer)
- **Job couvert :** TTS feedback agent (hooks Claude session-start déjà présents)
- **Pourquoi pour CE projet :** historique `.agentvibes` + hook TTS ; confort, pas critique ship
- **Pourquoi PAS un skill déjà là :** c’est le serveur TTS dédié
- **Install cible :** projet `.mcp.json` (déjà)
- **Méthode d’install (fix) :**
  ```bash
  pip3 install mcp
  # puis re-test
  grok mcp doctor agentvibes
  # Si toujours fail : retirer du .mcp.json pour tuer le bruit au boot
  ```
- **Auth / secrets :** provider TTS optionnel (selon config AgentVibes)
- **Privacy / risque :** OK si pas de decklist lue à voix haute vers un cloud TTS sans opt-in
- **Coût / quota :** free local / paid si provider cloud TTS
- **Smoke test :** handshake + un speak court
- **Décision recommandée :** **Fix** si TTS utile · sinon **Rejeter / retirer** du `.mcp.json`

---

### [chrome-devtools-mcp]

- **Priorité :** **P1**
- **Job couvert :** Core Web Vitals / perf / network dans Chrome
- **Pourquoi pour CE projet :** prerender + LCP Library/Analyzer ; perf regresse facilement
- **Pourquoi PAS un skill déjà là :** skills SEO parlent CWV mais sans CDP live
- **Install cible :** user-global
- **Méthode d’install :**
  ```bash
  grok mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest
  claude mcp add chrome-devtools -- npx -y chrome-devtools-mcp@latest
  ```
- **Auth / secrets :** aucune
- **Privacy / risque :** OK (local browser)
- **Coût / quota :** free
- **Smoke test :** ouvrir manatuner.app et lire LCP approx / network
- **Décision recommandée :** **Installer** après Playwright si perf devient un chantier

---

### [Discord MCP — plugin Claude officiel]

- **Priorité :** **P1** (distribution LAUNCH.md)
- **Job couvert :** poster / répondre dans Discord via bot pour distribution MTG
- **Pourquoi pour CE projet :** PRIORITÉ 2 LAUNCH = serveurs Discord existants
- **Pourquoi PAS un skill déjà là :** skills community-marketing = copy only ; pas d’API Discord
- **Install cible :** user-global Claude plugin (`/plugin install discord@claude-plugins-official`) ; Grok = équivalent bot custom si dispo, sinon **reporter** côté Grok
- **Méthode d’install :**
  ```text
  # Dans Claude Code
  /plugin install discord@claude-plugins-official
  /reload-plugins
  /discord:configure <BOT_TOKEN>
  # Relancer : claude --channels plugin:discord@claude-plugins-official
  # Pairing DM bot
  ```
- **Auth / secrets :** `DISCORD_BOT_TOKEN` dans `~/.claude/channels/discord/.env` (hors git)
- **Privacy / risque :** **attention** — ne jamais poster de decklist privée utilisateur ; contenu = screenshots / liens manatuner.app
- **Coût / quota :** free (bot Discord)
- **Smoke test :** DM bot → pairing → message test canal privé
- **Décision recommandée :** **Reporter** jusqu’à stratégie bot claire (sinon spam / ban risque) · **Installer** si tu as un bot prêt

---

### [Sentry MCP — `@sentry/mcp-server`]

- **Priorité :** **Skip / P2 gated**
- **Job couvert :** lire issues crash prod
- **Pourquoi pour CE projet :** SDK présent mais **DSN volontairement off**
- **Pourquoi PAS … :** rouvre décision owner Sentry (`SECURITY.md` 4 étapes)
- **Install cible :** n/a tant que DSN unset
- **Auth / secrets :** Sentry auth token
- **Privacy / risque :** **interdit sans owner** (aligné claims Privacy)
- **Coût / quota :** paid plan possible
- **Smoke test :** n/a
- **Décision recommandée :** **Rejeter** jusqu’à checklist privacy complète

---

### [Memory MCP — `@modelcontextprotocol/server-memory`]

- **Priorité :** **P2**
- **Job couvert :** knowledge graph persistant cross-sessions
- **Pourquoi pour CE projet :** sessions multi-jours ; **mais** HANDOFF.md / STATUS.md sont déjà la SSOT
- **Pourquoi PAS un skill déjà là :** fichiers session = source de vérité owner ; memory MCP risque de diverger
- **Install cible :** user-global
- **Méthode d’install :**
  ```bash
  grok mcp add memory -- npx -y @modelcontextprotocol/server-memory
  ```
- **Auth / secrets :** aucune (fichier local)
- **Privacy / risque :** OK si pas de decklists stockées
- **Coût / quota :** free
- **Smoke test :** store/recall une note « Engine v2.7.8 »
- **Décision recommandée :** **Reporter** — HANDOFF suffit

---

### [Sequential Thinking MCP]

- **Priorité :** **P2 / Skip**
- **Job couvert :** raisonnement structuré multi-étapes
- **Pourquoi pour CE projet :** audits math / persona complexes
- **Pourquoi PAS … :** Grok/Claude raisonnement natif + subagents + plan mode déjà là
- **Décision recommandée :** **Rejeter** (charge cognitive, peu de gain)

---

### [Firecrawl MCP]

- **Priorité :** **Skip**
- **Job couvert :** crawl sites
- **Pourquoi PAS :** Apify déjà là + skills firecrawl SEO + Grok browse ; doublon payant
- **Décision recommandée :** **Rejeter**

---

### [Filesystem MCP]

- **Priorité :** **Skip**
- **Job couvert :** lire/écrire fichiers
- **Pourquoi PAS :** tools natifs Grok/Claude (`read_file`, etc.) déjà supérieurs et scopés workspace
- **Décision recommandée :** **Rejeter**

---

### [Linear / Notion / Figma MCP]

- **Priorité :** **Skip**
- **Job couvert :** task board / design
- **Pourquoi PAS :** backlog ManaTuner = HANDOFF + GitHub Issues ; pas de Figma workflow actif
- **Décision recommandée :** **Rejeter** (sauf si tu bascules explicitement le backlog sur Linear)

---

### [Puppeteer / Browserbase MCP]

- **Priorité :** **Skip**
- **Pourquoi PAS :** doublon Playwright ; Browserbase = cloud + coût + privacy
- **Décision recommandée :** **Rejeter**

---

### [Database / Supabase MCP]

- **Priorité :** **Skip**
- **Pourquoi PAS :** Supabase **REMOVED** ; privacy-first localStorage only
- **Décision recommandée :** **Rejeter**

---

## 5. Synthèse priorisée

### 5.1 Tableau installable (max ~8–12)

| Prio | Serveur                                                                                | Cible         | Auth         | Décision                             |
| ---- | -------------------------------------------------------------------------------------- | ------------- | ------------ | ------------------------------------ |
| P0   | **playwright**                                                                         | projet + user | none         | Installer                            |
| P0   | **vercel**                                                                             | user          | OAuth        | Installer                            |
| P0   | **github** (remote)                                                                    | user          | OAuth/PAT    | Installer (ou garder `gh`)           |
| P0   | context7                                                                               | user (déjà)   | API key      | Garder + rotator clé                 |
| P1   | apify                                                                                  | user (déjà)   | token        | Garder + rotator + policy            |
| P1   | agentvibes                                                                             | projet        | optional TTS | Fix `pip install mcp` **ou** retirer |
| P1   | chrome-devtools                                                                        | user          | none         | Après Playwright si perf             |
| P1   | discord (Claude plugin)                                                                | user Claude   | bot token    | Si bot prêt pour LAUNCH              |
| P2   | memory                                                                                 | user          | none         | Reporter (HANDOFF SSOT)              |
| —    | sentry, firecrawl, filesystem, linear, figma, puppeteer, sequential-thinking, supabase | —             | —            | **Ne pas installer**                 |

### 5.2 À NE PAS installer (raisons)

| MCP / catégorie            | Raison                                                 |
| -------------------------- | ------------------------------------------------------ |
| SEO MCP (DataForSEO, etc.) | 50+ skills SEO/GEO déjà installées                     |
| Sentry MCP                 | DSN off + privacy owner                                |
| Analytics / tracking MCP   | claims Privacy + « ne pas rouvrir analytics decklist » |
| Supabase / DB              | backend retiré                                         |
| Filesystem                 | tools natifs                                           |
| Firecrawl                  | doublon Apify + browse                                 |
| Sequential thinking        | raisonnement natif + subagents                         |
| Figma                      | pas de DS Figma actif                                  |
| Browserbase cloud          | privacy + cost                                         |

### 5.3 Plan d’install en vagues

| Vague              | Contenu                                      | Effort    | Prérequis                 |
| ------------------ | -------------------------------------------- | --------- | ------------------------- |
| **A — 15 min**     | Playwright + (option) retirer/fix agentvibes | faible    | Node/npx                  |
| **B — auth**       | Vercel OAuth + GitHub OAuth/PAT              | moyen     | comptes Vercel + GitHub   |
| **C — nice**       | chrome-devtools · Discord bot · memory       | optionnel | bot Discord / besoin réel |
| **Sécu parallèle** | Rotation clés Context7 + Apify               | 10 min    | dashboards Context7/Apify |

### 5.4 Snippet `.mcp.json` projet proposé (post-`go install`)

**Actuel :**

```json
{
  "mcpServers": {
    "agentvibes": {
      "command": "npx",
      "args": ["-y", "--package=agentvibes", "agentvibes-mcp-server"]
    }
  }
}
```

**Proposé (Vague A — agentvibes retiré tant que fail ; playwright ajouté) :**

```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    }
  }
}
```

**Variante si TTS réparé :**

```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },
    "agentvibes": {
      "command": "npx",
      "args": ["-y", "--package=agentvibes", "agentvibes-mcp-server"]
    }
  }
}
```

**User Grok `~/.grok/config.toml` (Vague B — secrets via env, pas en clair) :**

```toml
# Ajoutés via `grok mcp add` — ne pas coller de tokens ici

# [mcp_servers.vercel]  → url = "https://mcp.vercel.com"
# [mcp_servers.github]  → url = "https://api.githubcopilot.com/mcp/"
# playwright peut rester projet-only ou aussi user
```

**User Claude (HTTP, sans commit) — conserver context7/apify, ajouter :**

```bash
claude mcp add --transport http vercel https://mcp.vercel.com
claude mcp add --transport http github https://api.githubcopilot.com/mcp/
claude mcp add playwright -- npx -y @playwright/mcp@latest
```

### 5.5 Commandes prêtes à coller (Phase 2 — **ne pas exécuter** tant que pas `go install`)

```bash
# === VAGUE A (P0 local, no auth) ===
cd "/Volumes/DataDisk/_Projects/Project Mana base V2"

# Backup
cp .mcp.json ".mcp.json.bak.$(date +%Y%m%d)"

# Playwright — projet
grok mcp add --scope project playwright -- npx -y @playwright/mcp@latest
claude mcp add playwright -- npx -y @playwright/mcp@latest

# Option: retirer agentvibes du bruit (éditer .mcp.json) OU tenter fix:
# pip3 install mcp && grok mcp doctor agentvibes

# Verify
grok mcp doctor
claude mcp list

# === VAGUE B (auth) ===
grok mcp add --transport http vercel https://mcp.vercel.com
grok mcp add --transport http github https://api.githubcopilot.com/mcp/
claude mcp add --transport http vercel https://mcp.vercel.com
claude mcp add --transport http github https://api.githubcopilot.com/mcp/
# Puis OAuth dans chaque client (/mcp, browser popup)

# === Reload agent ===
# Grok Build : nouvelle session / restart TUI dans le dossier projet
# Claude Code : quitter + `claude` ou /mcp reload selon version
# Cursor : Developer: Reload Window
```

### 5.6 Comment recharger l’agent (post-install)

| Client          | Action                                                                                     |
| --------------- | ------------------------------------------------------------------------------------------ |
| **Grok Build**  | Quitter la session, relancer `grok` dans le repo ; ou `/mcps` si dispo ; `grok mcp doctor` |
| **Claude Code** | Nouvelle session `claude` ; `claude mcp list` ; pour Discord channel : flag `--channels`   |
| **Cursor**      | Command Palette → **Developer: Reload Window**                                             |

---

## 6. Mapping axes d’analyse → décision

| Axe          | Conclusion                                                       |
| ------------ | ---------------------------------------------------------------- |
| Dev / code   | Context7 **OK** ; pas besoin d’autres doc MCP                    |
| Qualité      | **Playwright MCP** = gain #1 ; a11y via skills/tests existants   |
| Ops          | **Vercel + GitHub** P0 ; Sentry skip                             |
| Produit MTG  | Pas de MCP Scryfall ; Apify pour research only                   |
| Distribution | Grok X tools natifs ; Discord P1 si bot ; pas de spam MCP social |
| SEO / GEO    | Skills >> MCP — **zéro install SEO**                             |
| Knowledge    | HANDOFF SSOT ; memory P2                                         |
| Sécurité     | Rotation clés ; pas d’analytics ; scope minimal                  |

---

## 7. Phase 2 — checklist (quand tu diras `go install`)

1. Backup `.mcp.json` (+ export `~/.claude.json` mcpServers sans coller secrets dans git)
2. Install vague A un serveur à la fois
3. `grok mcp doctor` / `claude mcp list` après chacun
4. Smoke non destructif
5. Vague B OAuth (humain dans le navigateur)
6. Documenter VERT/ROUGE dans ce fichier (section append)
7. **Pas de commit secrets** ; commit config seule si `go commit`

---

## 8. Question unique

**Réponds par l’un de :**

- `go install` — vagues A + B (Playwright, Vercel, GitHub ; agentvibes fix/remove)
- `go install: P0` — Playwright + Vercel + GitHub seulement
- `go install: playwright,vercel` — liste libre
- `stop` — fin d’audit, aucune install

---

_Fin Phase 1 — aucune installation effectuée le 2026-08-01._

---

## 9. Phase 2 — Rapport d’installation (2026-08-01)

> Ordre humain : **install tout**  
> Backups : `.mcp.json.bak.20260801-140417` · `~/.claude.json.bak.*`

### 9.1 Résultat handshake

| Serveur                                                  | Statut                   | Tools | Notes                                                         |
| -------------------------------------------------------- | ------------------------ | ----- | ------------------------------------------------------------- |
| **playwright**                                           | **VERT**                 | 24    | Grok user + projet `.grok/config.toml` + Claude + `.mcp.json` |
| **chrome-devtools**                                      | **VERT**                 | 29    | user Grok + Claude                                            |
| **memory**                                               | **VERT**                 | 9     | user Grok + Claude                                            |
| **github** (stdio `@modelcontextprotocol/server-github`) | **VERT**                 | 26    | token via `${GITHUB_PERSONAL_ACCESS_TOKEN}` + `gh auth token` |
| **context7**                                             | **VERT**                 | 2     | déjà présent (clé encore en clair — **rotater**)              |
| **apify**                                                | **VERT**                 | 15    | déjà présent (token encore en clair — **rotater**)            |
| **vercel**                                               | **ROUGE — OAuth requis** | —     | Config OK ; auth interactive manquante                        |
| **agentvibes**                                           | **RETIRÉ**               | —     | Incompatible `mcp` Python 2.0 (`Server` sans `list_tools`)    |
| **discord**                                              | **NON INSTALLÉ**         | —     | Bot token + plugin Claude requis (action manuelle)            |

**Score doctor Grok final :** **6 healthy · 1 failing** (vercel OAuth)

### 9.2 Fichiers touchés

| Fichier                      | Action                                                                            |
| ---------------------------- | --------------------------------------------------------------------------------- |
| `~/.grok/config.toml`        | + playwright, vercel, chrome-devtools, memory, github                             |
| `.grok/config.toml` (projet) | + playwright                                                                      |
| `.mcp.json`                  | playwright only (agentvibes retiré)                                               |
| `~/.claude.json`             | + vercel, github, chrome-devtools, memory, playwright (proj) ; tokens GH scrubbés |
| `~/.zshrc`                   | helper `GITHUB_PERSONAL_ACCESS_TOKEN=$(gh auth token)`                            |

### 9.3 Auth encore requise (humain)

**Vercel (Grok + Claude) :**

```bash
# Claude Code — dans une session :
# /mcp  → sélectionner vercel → Authenticate (navigateur OAuth)

# Ou re-add puis auth
claude mcp add --transport http vercel https://mcp.vercel.com
# puis /mcp

# Grok Build : /mcps ou reconnect session après OAuth si supporté
# Doc : https://vercel.com/docs/agent-resources/vercel-mcp
```

**GitHub :** OK si `gh auth login` actif et shell charge `~/.zshrc`

```bash
source ~/.zshrc   # ou ouvrir un nouveau terminal
echo ${#GITHUB_PERSONAL_ACCESS_TOKEN}  # doit être > 0
```

**Discord (optionnel LAUNCH) :**

```text
# Claude Code
/plugin install discord@claude-plugins-official
/reload-plugins
/discord:configure <BOT_TOKEN>
# relancer: claude --channels plugin:discord@claude-plugins-official
# pair DM bot
```

**Rotation secrets (recommandé) :** Context7 + Apify dans `~/.claude.json` encore en clair.

### 9.4 Comment recharger l’agent

| Client          | Action                                                                  |
| --------------- | ----------------------------------------------------------------------- |
| **Grok Build**  | Quitter + relancer `grok` dans le repo ; vérifier `grok mcp doctor`     |
| **Claude Code** | Nouvelle session `claude` ; `claude mcp list` ; OAuth vercel via `/mcp` |
| **Cursor**      | Developer: Reload Window                                                |

### 9.5 Smoke tests non destructifs (à faire après reload)

```bash
export GITHUB_PERSONAL_ACCESS_TOKEN="$(gh auth token)"
grok mcp doctor          # viser 7/7 après OAuth Vercel
claude mcp list

# Après reload session — exemples d’appels :
# Playwright : ouvrir http://localhost:3000/analyzer?sample=edh
# GitHub     : lister issues gbordes77/manatuner
# Memory     : store "ManaTuner engine v2.7.8"
# Context7   : docs React 18 useMemo
```

### 9.6 AgentVibes — cause root (pour plus tard)

```
AttributeError: 'Server' object has no attribute 'list_tools'
# package agentvibes (npx) vs Python mcp 2.0.0 API break
# Fix amont : pin mcp<2 ou attendre patch agentvibes
# pip3 install --user --break-system-packages 'mcp<2'  # tentative non validée
```

### 9.7 Rollback

```bash
cd "/Volumes/DataDisk/_Projects/Project Mana base V2"
cp .mcp.json.bak.20260801-140417 .mcp.json
# Restaurer claude : ~/.claude.json.bak.*
# Grok : éditer ~/.grok/config.toml — retirer [mcp_servers.*] ajoutés
grok mcp remove playwright
grok mcp remove vercel
grok mcp remove github
grok mcp remove chrome-devtools
grok mcp remove memory
```

_Fin Phase 2 install — 2026-08-01._
