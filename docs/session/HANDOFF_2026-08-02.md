# HANDOFF — 2026-08-02 · Audit evolutions T02–T05

> **SSOT session technique (refactor hygiene).**  
> **Date :** 2026-08-02  
> **Repo :** https://github.com/gbordes77/manatuner  
> **Commit code :** **`332501d`** sur `origin/main`  
> **Prod live inchangée :** **v2.7.9** · SHA **`fdef163`** · https://www.manatuner.app  
> **Audit source :** [`../../AUDIT_EVOLUTIONS.md`](../../AUDIT_EVOLUTIONS.md) §7

---

## 0. Phrase à coller (nouvelle conversation)

```
Lis SESSION_START.md + docs/product/STATUS.md + docs/session/HANDOFF_NEXT.md
+ AUDIT_EVOLUTIONS.md §7 (état T02–T05 FAIT, commit 332501d).

ManaTuner : code main @ 332501d (hygiène T02–T05) ; prod live encore v2.7.9 @ fdef163
sauf si deploy fait depuis.

Priorité business = LAUNCH.md (distribution). Pas de feature gratuite.
Suite tech optionnelle = T06+ de AUDIT_EVOLUTIONS.md — seulement sur ordre explicite.
Ne pas activer VITE_SENTRY_DSN sans checklist SECURITY.md.
Ne pas rouvrir Moxfield URL, i18n FR, backend, analytics decklist sans owner.
```

---

## 1. Résumé 30 s

|                    |                                                                                          |
| ------------------ | ---------------------------------------------------------------------------------------- |
| **Mission**        | Exécuter **uniquement** T02 → T05 de l’audit evolutions (refactor comportement constant) |
| **Résultat**       | ✅ **T02 T03 T04 T05 FAIT** · arbre vert · push `332501d`                                |
| **Tests unit fin** | **384** pass / 2 skip                                                                    |
| **Prod**           | **Pas** de ship de ce commit sauf ordre « go prod »                                      |
| **Suite**          | **LAUNCH.md** (P0 business) **ou** T06+ si owner le demande                              |

---

## 2. Détail par tâche

### T02 — Purge code mort (§7.2, 5 actions) — ✅

1. **`manaCalculator.ts`** — supprimé `calculateProbabilityByTurn`, `analyzeDeckConsistency`, `calculateOptimalLandCount` + interfaces mortes. Conservé `calculateHypergeometric` + `ManaCalculator` + `KARSTEN_TABLES`.
2. **`londonMulligan.test.ts`** — imports → `mulliganSimulatorAdvanced` ; strategy test → `analyzeWithArchetype` + shape `AdvancedMulliganResult`.
3. **Fusion tests manaCalculator** — hypergeom + Karsten dans `src/services/manaCalculator.test.ts` ; supprimé `src/services/__tests__/manaCalculator.test.ts`.
4. **`main.tsx`** — React Query retiré (provider, client, DevTools).
5. **`package.json`** — deps mortes retirées + `npm install` (−14 packages).

**Conservé (amendement audit) :** `public/sw.js` + headers vercel (SW killer).

**Build post-T02 :** `dist/workers/` absent · zéro chunk react-query / react-window / @mui/lab.

| Chunk `dist/assets` |   Avant T02 |   Après T02 |                         Δ |
| ------------------- | ----------: | ----------: | ------------------------: |
| `index-*.js`        |   140 935 B |   116 897 B |             **−24 038 B** |
| `vendor-mui-*.js`   |   518 614 B |   518 605 B |                        ~0 |
| `vendor-react-*.js` |    20 750 B |    37 707 B | +16 957 B (re-chunk Vite) |
| **Total assets**    | 2 589 155 B | 2 599 367 B |                   +~10 KB |

### T03 — SSOT terrains — ✅

- API sync `landService` : `getLandSync` / `isLandSync` / `getCategoryLabelSync`
- `landUtils.ts` = wrappers (listes hardcodées + keywords **supprimés**)
- `deckAnalyzer` shock/fast/rainbow via `category` / `etbBehavior`
- Seed **210 → 259** (+ legacy non-régression)
- Tests : landUtils labels mis à jour · `landDetection.ssot.test.ts` + fixture 179 noms uniques

### T04 — Batch landCache — ✅

- Load 1× mémoire · `set` dirty · flush fin preload / debounce 500 ms + idle / `beforeunload` + `visibilitychange`
- QuotaExceeded → éviction oldest + retry 1× · JSON corrompu → cache vide
- Tests : `landCacheService.test.ts` (5)

### T05 — fetchWithTimeout — ✅

- `src/services/http.ts` : timeout 8 s · retry 429/5xx · Retry-After · signal externe · cleanup timers
- Branché : `scryfall.ts`, `deckAnalyzer.ts`, `manaProducerService.ts`
- Zéro `fetch(` restant dans `src/services/` hors `http.ts`
- Tests : `http.test.ts` (6)

---

## 3. Validation globale session

| Commande     | T02     | T03 | T04 | T05       |
| ------------ | ------- | --- | --- | --------- |
| `type-check` | ✓       | ✓   | ✓   | ✓         |
| `test:unit`  | ✓       | ✓   | ✓   | ✓ **384** |
| `lint`       | ✓ 0 err | ✓   | ✓   | ✓         |
| `build`      | ✓       | —   | —   | —         |

**Commit push (sur demande user) :** `332501d` → `origin/main`  
**Docs session + AUDIT §7** : mis à jour 2026-08-02 (ce handoff + docs listées en §5) — à committer si demandé.

---

## 4. Écarts justifiés vs prompts §3

| Tâche | Écart                                              | Pourquoi                                                                       |
| ----- | -------------------------------------------------- | ------------------------------------------------------------------------------ |
| T02   | `public/sw.js` **conservé**                        | Amendement §7.1 (SW killer)                                                    |
| T02   | Fixes TS `MyAnalysesPage` + `assertCardResolution` | Bloquants type-check préexistants                                              |
| T03   | Label « Rainbow Land » → **Utility Land**          | Catégorie seed SSOT                                                            |
| T05   | Pas de retry réseau générique                      | Aligné prompt (429/5xx only) ; ancien tryFetch retryait aussi les catch réseau |

---

## 5. Docs mises à jour cette session (post-code)

| Fichier                              | Rôle                                        |
| ------------------------------------ | ------------------------------------------- |
| `AUDIT_EVOLUTIONS.md` §7             | Journal **FAIT** T02–T05 + tailles + écarts |
| `docs/session/HANDOFF_2026-08-02.md` | Ce handoff                                  |
| `docs/session/HANDOFF_NEXT.md`       | Priorités suite + SHA code                  |
| `docs/product/STATUS.md`             | Vague J hygiène, tests 384                  |
| `Claude.md`                          | Latest block                                |
| `CHANGELOG.md`                       | `[Unreleased]` T02–T05                      |
| `docs/README.md`                     | Index pointant handoff 08-02                |

---

## 6. Suite (ne pas faire sans ordre)

1. **P0 business** — `LAUNCH.md` (distribution)
2. Optionnel tech — **T06** tempo non bloquant → T07 batch lands inconnus…
3. **go prod** pour `332501d` seulement si owner demande (prod live encore `fdef163` v2.7.9)

---

## 7. Interdictions rappel

Pas de commit/push sans ordre · pas de `.env`/secrets · pas T06+ sans ordre · pas activer Sentry DSN sans checklist privacy.
